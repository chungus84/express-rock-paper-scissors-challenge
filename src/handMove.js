export const handMove = Object.freeze({
    rock: 'rock',
    paper: 'paper',
    scissors: 'scissors',
    lizard: 'lizard',
    spock: 'spock'
});

// const { Lizard, Spock, ...simpleHands } = handMove;

// console.log(simpleHands);
